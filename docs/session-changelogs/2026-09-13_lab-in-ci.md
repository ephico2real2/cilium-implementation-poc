# Session change log — cilium-implementation-poc, 2026-09-13 → 2026-09-14

What this session did, in order, with the commit, run or measurement behind each line. Times are the git
author times on this machine (America/Chicago); run ids are GitHub Actions runs of `.github/workflows/lab-spike-kind.yaml`.
Every "measured" claim is one the session ran a command for; nothing below is recalled from memory alone.
The session starts at the cf2cnp 0.7.0 review (commit `98fc8ab`); the day's earlier commits (demos 29–35,
the plans for enhancements 002 and 003) belong to the previous session, which had no log of this kind.

Outcome in one line: **cf2cnp 0.7.0 and the verdict dashboard 0.4.0 released after two-reviewer passes;
the lab moved to GitHub Actions and, after sixteen runs, builds two complete and independent kind
clusters, meshes them, and passes Cilium's own multi-cluster suite — 87 of 87.**

| | Before the session | After |
|---|---|---|
| cf2cnp (the fork) | 0.6.1 under review, enhancement 003 open | **0.7.0** released (chart, image, binaries), deployed on poc1, enhancement 003 done |
| hubble-policy-verdicts | 0.3.0 | **0.4.0**, six numbered sections, a CI contract script; deployed beside cf2cnp 0.7.0 |
| the lab | on the MacBook only; Prometheus killed by its probe under load | `scripts/lab-up.sh` on a runner: meshed in 445 s, `All 87 tests successful`; the MacBook paused |
| `docs/GOTCHAS.md` | 89 traps | **104 traps** (#90–#104), each with its run or capture |
| enhancement 004 | not written | researched, decided (kind), phase 0 green; phases 1–3 ahead |

---

## Part 1 — cf2cnp 0.7.0 and the dashboard 0.4.0 (2026-09-13, 12:23 → 16:12)

### The releases (16:12) — commit `98fc8ab`

- cf2cnp 0.7.0 shipped on Cilium's own policy types after two adversarial passes; the record is
  `docs/REVIEW_ENH-003.md`: thirteen claims, Cursor refuted five and Codex five, the accepted fixes
  applied with failing-first tests, Codex's second pass found three more (a wrong `apiVersion`
  accepted, a refusal without its reason, `merge` without `--l7`), all applied; its verdict before them
  was "do not release 0.7.0 unchanged", after them every finding closed or recorded.
- The verdict dashboard 0.4.0: the record is `docs/REVIEW_DASHBOARD-0.4.0.md` — nine claims, five refuted
  by both reviewers on the same points, every accepted fix held by the chart's contract script.
- Two traps written down on the way: the chart's namespace dashboards carry a `kubecon-demo` tag nothing
  removes (#90), and Prometheus killed itself every few minutes on the busy laptop — the liveness probe,
  not the queries (#91).

---

## Part 2 — the lab in CI, phase 0 (2026-09-13, 16:15 → 2026-09-14, 05:37)

### The plan and the decision (16:15 → 16:27) — commits `61078c9`, `c0cc4dc`

- The operator: "Why are we fighting all this" — the lab goes to GitHub Actions the way fluentd-hec's
  workflows do. Researched and written as `enhancements/004-lab-in-ci.md`: public repository →
  4 vCPU / 16 GB runners; minikube cannot mesh two profiles (kubernetes/minikube#14799, closed not
  planned; `--static-ip` broken with `--network`, #19284); the host-proxy workaround reaches a control
  plane only. **Decided:** kind, on the pattern of Cilium's own conformance-clustermesh workflow.

### Runs 1–11: the bring-up learns the runner (16:30 → 19:12) — commits `3dee5f3` … `01b4aa2`

Each run read from its log or sysdump, each fix at its layer; the traps are #95–#103 with their runs:

- **Found by CI**, run 34784194103: a Helm change to Cilium's ConfigMap rolls nothing; the agents died on
  the next restart wanting Gateway API CRDs (#97) → the CRDs first, Gateway API in the one install.
  Same run: BIG TCP refused under VXLAN (#103).
- **Found by CI**, run 34785181164: the bandwidth manager off in kind on any kernel — a host-namespace
  sysctl (#103); netkit and dual-stack work on the runner's 6.17 kernel.
- **Found by CI**, run 34785675472 (cancelled by hand after 23 minutes): the connectivity test needs a
  second schedulable node (#100) → the control-plane taint removed through kubeadm patches for both
  apiVersions; the step streamed, with a 45-minute limit.
- **Found by CI**, run 34787222878: pods could not resolve an external name — `Resolving timed out`
  (#98) → CoreDNS on explicit upstreams. The same run's sysdump listed every piece of the laptop lab the
  CI lab lacked: the ten Gateway API CRDs (now vendored under `crds/` with `scripts/gateway-api-crds.sh`),
  Tetragon, cert-manager with the root, metrics-server.
- **Found by CI**, run 34789240547: the egress gateway and CiliumEndpointSlice refuse to run together,
  and `| tee` had made the step green past the crash (#101) → CES out, `pipefail` in.
- **Found by CI**, run 34790002220: Helm `--wait` blocked ten minutes on the Hubble UI's LoadBalancer
  before its pool existed (#99). **The operator:** "Another bug and stupid steps" — "each cluster must be
  fully functional and IP address pool done and cert manager installed" → `scripts/lab-up.sh` rewritten
  in dependency order (`4c87f9e`).
- **Found by CI**, run 34790879335: the chart's validate template refuses a relay without Hubble (#102).
- **Found by CI**, run 34791073921: the mesh apiserver `Init:0/1` for ten minutes on a users ConfigMap
  the chart renders only when the mesh is declared (#96) → demo 24's shape, every member declared from
  live addresses.
- **The operator:** "You still need to set the cidr to the kind docker bridge network. Pls google first."
  → kind sets no IPv4 subnet and reuses any `kind` network (#95); the network is created first, with
  `--ip-range` holding containers to the lower half (`7108157`).
- `scripts/lab-route.sh` (`d7aae16`): the pool addresses reachable from the host, Linux and macOS.

### Runs 12–13: the same wait, twice (19:12 → 19:55) — commits `80db3be`, `bed849f`

- **Found by the operator** watching the log: "things just stop here and stay for like forever". Both
  runs spent the whole 10-minute `cilium status --wait` on `controller remote-etcd-poc2 … not found` —
  poc1 was completed "with the mesh" while poc2 had no Cilium (#92). First cut: a check that tolerates
  the peers not yet complete.
- **Found by the operator** reading the same log: "A cluster is independent before it joined a mesh and
  this is true for the pool IPs reserved as well. So poc2 must have its own." One pool file had been
  applied to every cluster (#94) → the reserved `172.18.255.0/24` subdivided into /26 blocks, one layout
  in each: poc1 keeps `.192/26` with every documented address unchanged, poc2 gets `.128/26`
  (`cilium/lb-ippool-poc2.yaml`) with its own L2 policy; `NETWORKING_DESIGN.md` §3 item 4 has the table.
- **Found by CI**, run 34792046715: the DNS probe's warning printed right after the answer it warned
  about — `grep -m1` under `pipefail` (#93) → captured then searched, six probes across the roll.

### Run 14: the first bring-up to go through (20:02 → 20:32) — commits `6176e5f`, `fa5ae2e`

- Measured: `lab up: poc1 poc2` in 500 s and 566 s; `All 2 nodes are connected to all clusters`;
  poc2's `rebel-base-lb` at `172.18.255.136` — its own block — HTTP 200 from the runner; 86 of 87
  connectivity tests.
- **Found by Cilium's log check:** `Failed waiting for clustermesh synchronization` in poc1's agents,
  logged at the restart that happened before poc2 existed. **The operator:** "we need both clusters to be
  up and independent before creating or running clustermesh steps or scripts" → the mesh became a phase
  after every cluster is complete (`mesh_up`); the tolerant check removed.
- **Found by the operator:** a Python `SyntaxError` in `scripts/lab-route.sh` (an f-string with escaped
  quotes) and `ARP/ping … no answer` beside a working Gateway. Measured: three addresses served HTTP 200
  and none answered ICMP — an L2-announced address has no owner to reply (#104) → both probes are TCP
  then the neighbour table.
- Also from that run: the bandwidth manager's warning failed the log check on every features run (out of
  the overlay); one `Error initially creating lease lock` at a rollout restart (client-go's create race,
  an explicit exception with its reason).

### Run 15: green (20:32 → 21:07) — commit `5467ec5`

- Measured, run 34796271073: poc1 complete and independent at 3 min 23 s, poc2 at 5 min 49 s, the mesh
  phase 33 s + 11 s, `lab up: poc1 poc2` in **445 s**; **`All 87 tests (512 actions) successful`** on
  `base`, `All 87 tests (816 actions) successful` on `kernel-features`; the log check clean.
- **Asked by the operator:** the twelve `cilium.io/v2alpha1 CiliumCIDRGroup is deprecated` warnings —
  traced to cilium-cli's own `client-egress-to-cidrgroup-deny` tests (49 and 50 of 137), which apply
  their v2alpha1 manifest on Cilium newer than 1.17.0; cf2cnp writes only `cilium.io/v2`
  CiliumNetworkPolicy and never a CIDRGroup (measured: 32 of 32 generated policies in the repo). The
  `⌛ Waiting for pod … to reach DNS server` lines are the test's readiness phase, not errors.
- **Asked by the operator:** "You should have installed cilium cli 1.20.1" — measured: no such release
  exists (the CLI is 0.x; v0.20.0 is what Cilium 1.20.1's docs install; the CLI source was removed from
  the v1.20 branch, commit `aa1a2bbf4` upstream).

### Where it stands, and the MacBook (2026-09-14, 01:14 → 05:37) — commits `c2a7dba`, `2b4f3eb`

- **Asked by the operator:** "are we done with our testing in the github action?" — no: a status table at
  the top of enhancement 004 says phase 0 is green, the stacks script is unwritten, phase 2 (the lab's own
  tests and captures) and phase 3 are not started, and the bring-up has had no review pass.
- **Asked by the operator:** the old Intel MacBook's limits on an M5 Pro. Researched: the `6.6.12` kernel
  was Docker Desktop 4.27.2's (February 2024, measured on this machine); Desktop 4.89.0 ships kernel
  v7.0.12 (release notes) on both CPUs; netkit needs ≥ 6.8 and `CONFIG_NETKIT` (Cilium 1.20.1 system
  requirements); the pinned node image digest is the OCI index carrying `linux/amd64` and `linux/arm64`
  (read from the registry). `scripts/lab-preflight.sh` measures every row on the machine before a cluster
  exists; **measured on the runner**, run 34834151349: netkit ok (6.17, `CONFIG_NETKIT` in `/boot/config`),
  Tetragon's symbol ok, node image `amd64 arm64`, IPv6 on a docker network ok, then `lab up` in 455 s.
  The macOS rows run only when Docker is up; they are the M5's first command.

---

## Part 3 — Docker Desktop on the Intel MacBook (2026-09-14, 06:30 →)

### The VM shrunk, the upgrade prepared (06:30 → 06:45) — no commit; measured on the machine

- **Recorded before any change** (Docker Desktop 4.27.2, quit; `settings.json` of 2026-09-10): 16 CPUs,
  16384 MiB memory, 1024 MiB swap, 61035 MiB disk (`Docker.raw` 60 GB apparent, 48 GB on disk),
  `kernelForUDP` on, Apple Virtualization framework with VirtioFS, autoStart off; the host has 16 CPUs
  and 32 GiB.
- **Changed, the operator's ask:** 2 CPUs, 8192 MiB, nothing else; the file read back and diffed against
  its backup (`settings.json.before-4.27.2-shrink-2026-09-14` beside it). Docker Desktop reads it at its
  next start.
- **How it was installed, measured:** Homebrew cask `docker-desktop` (4.27.2,137060; the Caskroom's
  `docker` is a symlink to it; no App Store receipt; no download URL on the bundle). Latest cask:
  4.90.0,238679.
- **The upgrade, first attempt:** `brew upgrade --cask docker-desktop` fetched the 641 MB dmg, then the
  cask's uninstall stanza stopped on `sudo` for the root-owned helpers (`com.docker.socket`,
  `com.docker.vmnetd` under `/Library/PrivilegedHelperTools`): "a terminal is required to read the
  password". The app is intact at 4.27.2; the dmg is cached; the operator runs the same command in a
  terminal.

### The MacBook measured, and a favour in another repository (07:00 → 07:20)

- **Measured:** load average 8.1 on 16 CPUs, 31 GB of 32 used, 15.8 GB of the 17.4 GB swap in use, xcpm
  CPU thermal level 76 with no speed or scheduler limit applied (`pmset -g therm`). The consumer is
  CRC's VM: `com.apple.Virtualization.VirtualMachine` at 360 % CPU and 19.9 GB resident, up 12 hours
  (`crc status`: OpenShift 4.18.2 running), plus `podman-machine-default` (applehv, 8 CPUs, 2 GiB)
  running. Docker Desktop is quit. Reported to the operator; nothing stopped.
- **signal_system** (the operator's other repository, commit `60fb373` there): `make stop` and
  `make system-down` only unload the five launchd jobs for the current login — the RunAtLoad plists stay
  in `~/Library/LaunchAgents` (measured: `launchctl list` empty, five symlinks present, no override). Added
  `make system-disable` / `system-enable` / `system-enabled-status` on `launchctl disable user/<uid>/<label>`,
  proven first with a throwaway label (a disabled label refuses both `launchctl load` — which still exits
  0 — and `bootstrap`); ran `system-disable`: six labels disabled, Redis included; `make start` and
  `make system-up` now refuse with the fix named.

### The review pass on the bring-up (07:30 → 11:35) — commits `2edeaf5`, `4618cc0`

- Ten claims to Cursor and Codex on copies of `875b5d2`; record `docs/REVIEW_ENH-004.md`. **Found by Codex:** the
  connectivity step had no `pipefail` — run 34794243096's jobs were green with `1/87 tests failed`; the agents
  read the declared peers through a watcher, so the mesh restart was unnecessary; a rerun's core install reset
  the release. **Found by Cursor:** the L2 line printed the MAC twice (`${mac:+…}${mac:-…}`), `grep -c || echo 0`
  yields `0\n0`, macOS `netstat` abbreviates the destination, route B's `connect` on a declared list is demo 13's
  failure. **Both:** the empty-`MemTotal` arithmetic abort. **Rejected:** Cursor's generation-only restart guard,
  Codex's "no restart at all", Codex's `--connection-mode mesh`. Every fix tested on this Mac with stubs (T1–T7).
- **Found by CI**, run 34864652168: route B's copied `cilium-ca` is not Helm's ("cannot be imported into the
  current release: invalid ownership metadata") → the first cluster's CA passed as `tls.ca.cert`/`tls.ca.key`.
- Measured on the fixed head: route A `lab up` 480 s / 542 s, `All 87 tests successful` on both jobs
  (run 34864635454); route B 359 s / 504 s, `cilium-ca fingerprint identical`, `All 87 tests successful` on both
  jobs (run 34865986688) — the first route-B run anywhere but the laptop.
- Also today: `docs/DOCKER-DESKTOP-RUNBOOK.md`, the preflight's CPU row, the next-steps table in 004, and in
  signal_system the launchd disable targets (its own commit `60fb373`).

### Route B end to end, the tutorials' checks in CI (11:40 → 12:45) — commits `ce9cfaa`, and the one after

- **The operator:** clone the route-A workflow under another name for route B, end to end, so the CI jobs
  validate the tutorials too — the clusters set up and the mesh available first, route B's checks only then.
  `lab-route-b.yaml`: the bring-up on Helm certificates, then SETUP 9.3b (one CA behind both clusters' mesh
  certificates, read back and compared), 9.5 (`clustermesh status --wait`), demo 07 Part 4 (the global service
  spread) and Part 5 (failover), then Cilium's suite.
- **Measured**, run 34872720896, both jobs green: `lab up` 419 s / 485 s; one CA; spread 8/12 and 10/10; failover
  **18 of 20** answered by poc2 and none by poc1 — the guide's 20/20 was measured after settling, CI's client ran
  5 s after the scale-down; the check now waits for the EndpointSlice to empty and prints the count as it is;
  `All 87 tests successful` on both jobs.
- **The operator:** no base job in `lab-route-b.yaml` — one job, the kernel-features shape (`f4a01ac`). Run
  34879584075 (87/87) showed the two missing answers in Part 4 as well as Part 5, so they were the measuring pod's
  own start-up, not the mesh; the client is now one pod, waited for and warmed, `exec`'d as the guide does
  (`6041950`): run 34882806257 measured spread **10 / 10** and failover **20 of 20 by poc2** — the guide's numbers.

---

## Numbers

| | |
|---|---|
| Commits authored (this session, `98fc8ab`..`2b4f3eb`) | 28 |
| Runs dispatched / green | 22 / 8 (runs 15, 16, the two on the reviewed head, three route-B end-to-end runs; run 14 through the bring-up only, its suite failure masked) |
| Review passes run | 4 (cf2cnp 0.7.0 twice, the dashboard once, the bring-up once — `docs/REVIEW_ENH-004.md`) |
| Reviewer findings accepted / rejected | in the two records above; every decision written there |
| Defects found by CI rather than reviewers | 12: traps #92–#103 less #94, and route B's copied CA |
| Reviewer findings accepted / rejected (the bring-up) | 10 accepted (8 on the claims, 2 volunteered) / 3 rejected |
| Defects found by the operator reading the log | 4: the mesh before the peer, one pool for two clusters, the route script's Python, "both clusters independent first" |
| Cilium's connectivity test, final | `All 87 tests successful`, both matrix entries |
| Longest single loss | runs 12 and 13: four jobs × 11 minutes on a peer that did not exist — written down as #92 and as the rule "the mesh is a phase on complete clusters" |

## Where things are recorded

- `docs/GOTCHAS.md` #90–#104; `README.md` (104 traps).
- `enhancements/004-lab-in-ci.md`: the facts, §2.1's measurements per run, the decision, the dependency
  order, the status table, phase 4 for the M5.
- `docs/REVIEW_ENH-003.md`, `docs/REVIEW_DASHBOARD-0.4.0.md`, `docs/POLICY-TEST-RESULTS.md`.
- `NETWORKING_DESIGN.md` §3 item 4 (the address plan), §4.2 and §5.1 (create the network first);
  `docs/SETUP.md` Step 2 (the Apple silicon note), Step 8, Step 9.2b.
- `scripts/lab-up.sh`, `scripts/lab-preflight.sh`, `scripts/lab-route.sh`, `scripts/lab-down.sh`,
  `scripts/gateway-api-crds.sh`, `crds/gateway-api/v1.6.1/`, `clusters/ci/`, `cilium/values-ci*.yaml`,
  `cilium/lb-ippool-poc1.yaml`, `cilium/lb-ippool-poc2.yaml`, `.github/workflows/lab-spike-kind.yaml`.
- Memory notes (outside the tree): *cilium-poc-lab-moves-to-ci* (status and resume point),
  *cilium-lab-each-cluster-independent* (the operator's rule).

## State left behind

- The MacBook's poc1 and poc2 are paused (`scripts/cluster-pause.sh`), every lab container is
  `--restart=no`, Docker Desktop is quit; nothing restarts with the machine.
- On GitHub: no run in progress; main at the commit after `4618cc0`, clean; Docker Desktop 4.91.0 installed on the Intel MacBook, not yet launched.
- Owed: phase 1's stacks script; phase 2's per-group workflow with
  captures; phase 3; the operator's schedule question (004 §5 item 2); on the M5, `scripts/lab-preflight.sh`
  first, then `scripts/lab-up.sh poc1 poc2`.
