# One MD5 password for every eBGP session on this fabric (leaf–spine–edge
# and the leaves' SERVERS group). The committed frr.conf files carry
# ${FABRIC_BGP_PASSWORD} only; the entrypoint renders this value at start.
# Override locally; do not put a production secret here.
FABRIC_BGP_PASSWORD=lab-bgp
