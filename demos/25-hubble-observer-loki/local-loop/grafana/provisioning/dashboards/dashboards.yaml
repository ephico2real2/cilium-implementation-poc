apiVersion: 1
providers:
  - name: hubble-observer
    folder: Hubble
    type: file
    updateIntervalSeconds: 10            # edit the JSON, wait ten seconds, reload the page
    allowUiUpdates: false
    options: {path: /var/lib/grafana/dashboards, foldersFromFilesStructure: false}
