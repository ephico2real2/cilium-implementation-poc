# Captures not taken yet — demos/13-ztunnel

poc4, the throwaway cluster ztunnel was evaluated on, was deleted (ztunnel cannot run with a `cluster.id`, so never on the meshed clusters). Captures to add if rebuilt: the WireGuard vs ztunnel encryption status and the −73 % throughput comparison.

When they are taken, put them in this folder and link them from the demo README's **Evidence** section; then remove this file and the row in `/missing-captures.md`.
