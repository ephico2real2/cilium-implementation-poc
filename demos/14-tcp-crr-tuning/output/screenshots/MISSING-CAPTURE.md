# Captures not taken yet — demos/14-tcp-crr-tuning

the tuning runs need a quiet VM (they measure TCP_CRR under load); the numbers are in the transcript. Captures to add: netperf output before/after each sysctl on a VM with headroom.

When they are taken, put them in this folder and link them from the demo README's **Evidence** section; then remove this file and the row in `/missing-captures.md`.
