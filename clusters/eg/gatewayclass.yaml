# GatewayClass for Envoy Gateway. Phase 0 measured that gateway-helm v1.9.1 does
# NOT create this (docs/EG-PHASE0.md item 3); apply it after the controller is up.
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: eg
spec:
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
